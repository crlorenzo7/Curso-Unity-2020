using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScreenController : MonoBehaviour
{
    // Start is called before the first frame update
    [SerializeField]
    public GameDimension dimensions;

    // Start is called before the first frame update
    void Start()
    {
    }

}
